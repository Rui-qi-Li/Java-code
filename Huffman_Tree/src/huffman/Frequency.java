package huffman;
import java.util.*;

public class Frequency{
		
		int frequency[] = new int[256];
		Arrays.fill(frequency, 0);
		for(int i=0;i<array.length;i++) {
			frequency[array[i]]++;
		}
		
	
}