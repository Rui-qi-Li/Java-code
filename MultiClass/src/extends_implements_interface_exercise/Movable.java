package extends_implements_interface_exercise;

public interface Movable
{
	//legal modifier for interface method: public, abstract, static,default
	public void move(int distance);
	public boolean canMove();
}